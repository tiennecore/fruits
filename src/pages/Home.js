import React from 'react';
import Navigation from "../components/nav/Navigation";

const Home = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Home;